org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# Claude API key for local/dev builds only. Never commit a real key here.
# Production builds must source this from a backend proxy, not the APK.
# See data/claude/ClaudeApiClient.kt for the isolation point.
SMR_CLAUDE_API_KEY=
