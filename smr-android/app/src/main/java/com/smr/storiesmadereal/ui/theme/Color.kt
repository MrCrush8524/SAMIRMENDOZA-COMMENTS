package com.smr.storiesmadereal.ui.theme

import androidx.compose.ui.graphics.Color

// Locked design palette: dark dreamy pastel, cobalt bloom behind the book cover.
object SmrPalette {
    val Base = Color(0xFF0D1326)
    val BaseElevated = Color(0xFF141B36)
    val BaseSunken = Color(0xFF080C1A)

    val Lavender = Color(0xFFAAA0D8)
    val LavenderDim = Color(0xFF7A7099)
    val Teal = Color(0xFF76A9A4)
    val TealDim = Color(0xFF4F7672)
    val Cream = Color(0xFFF4F4E8)
    val CreamDim = Color(0xFFB9B9AE)

    // Cobalt glow radiating from behind the book cover on Now Playing.
    val CobaltGlowCore = Color(0xFF3A5CC7)
    val CobaltGlowEdge = Color(0x003A5CC7)

    val Divider = Color(0x33AAA0D8)
    val Error = Color(0xFFE49A9A)
}
