{
  "name": "amb-android",
  "version": "2.7.0",
  "private": true,
  "description": "Anthony's Music Box — Android app (Capacitor wrapper around ../anthonys-music-box)",
  "scripts": {
    "copy-web": "node scripts/copy-web.mjs",
    "sync": "npm run copy-web && cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/app": "^6.0.2",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
